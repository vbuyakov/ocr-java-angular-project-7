# OrionCRM - Environment variables
# Copy to .env and adjust for your environment: cp .env.example .env

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
# Spring Boot application name (backend)
SPRING_APP_NAME=microcrm

# -----------------------------------------------------------------------------
# Docker Compose
# -----------------------------------------------------------------------------
# Port exposed by nginx (80 = dev; 8080 = prod with host Nginx to avoid conflict)
APP_PORT=80

# -----------------------------------------------------------------------------
# Docker network
# -----------------------------------------------------------------------------
# Network name for orion-microcrm services (default: orion-microcrm_network)
DOCKER_NETWORK=orion-microcrm_network

# Set to "true" when using an external reverse proxy - network must exist
# Create with: docker network create orion-microcrm_network
DOCKER_NETWORK_EXTERNAL=false

# -----------------------------------------------------------------------------
# ELK Stack (docker-compose-elk.yml)
# -----------------------------------------------------------------------------
# Project prefix for containers, network, volumes (avoid conflicts when multiple ELK on same host)
ELK_PROJECT=ocr-ja7

# Kibana port on host - change for each ELK stack (5601, 5602, 5603...)
KIBANA_PORT=5601
# Public URL for Kibana (links, exports) - no trailing slash
# KIBANA_PUBLIC_URL=https://ocr-ja7-elk.buyakov.com

# ELK security (required when xpack.security.enabled=true)
# Password for elastic user - used by Kibana and Logstash to connect to Elasticsearch
ELASTIC_PASSWORD=StrongPasswordHere
